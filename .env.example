# ═══════════════════════════════════════════════════════════════
# Jarvis OS — Environment Configuration
# Copy this file to .env and fill in your values
# ═══════════════════════════════════════════════════════════════

# ── LLM Provider (required — pick at least one) ──────────────
# Options: openai, anthropic, ollama
LLM_PROVIDER=openai

# OpenAI
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o

# Anthropic (alternative)
# ANTHROPIC_API_KEY=sk-ant-your-key-here
# ANTHROPIC_MODEL=claude-sonnet-4-20250514

# Ollama (local, no key needed)
# OLLAMA_HOST=http://ollama:11434
# OLLAMA_MODEL=llama3

# ── Agent Settings ───────────────────────────────────────────
AGENT_NAME=Jarvis
AGENT_PORT=8080
AGENT_LOG_LEVEL=INFO

# API key for the Jarvis HTTP API (set a strong random string)
AGENT_API_KEY=change-me-to-a-strong-secret

# ── Memory ───────────────────────────────────────────────────
MEMORY_BACKEND=sqlite
# For PostgreSQL:
# MEMORY_BACKEND=postgres
# DATABASE_URL=postgresql://jarvis:password@db:5432/jarvis

# Vector store for semantic memory
VECTOR_STORE=chromadb
# For Pinecone:
# VECTOR_STORE=pinecone
# PINECONE_API_KEY=your-key
# PINECONE_INDEX=jarvis-memory

# ── Integrations (all optional) ──────────────────────────────

# Slack
SLACK_BOT_TOKEN=
SLACK_APP_TOKEN=
SLACK_SIGNING_SECRET=

# Twitter / X
TWITTER_API_KEY=
TWITTER_API_SECRET=
TWITTER_ACCESS_TOKEN=
TWITTER_ACCESS_SECRET=

# GitHub
GITHUB_TOKEN=

# Email (SMTP)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=

# Webhooks (receive external triggers)
WEBHOOK_SECRET=

# ── Advanced ─────────────────────────────────────────────────
# Max concurrent tool executions
MAX_CONCURRENT_TOOLS=5

# Memory retention (days, 0 = forever)
MEMORY_RETENTION_DAYS=365

# Code execution sandbox timeout (seconds)
CODE_EXEC_TIMEOUT=30

# Rate limit for API (requests per minute)
API_RATE_LIMIT=60
